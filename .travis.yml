language: node_js
node_js:
  - "stable"
sudo: false
dist: trusty
addons:
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable

services:
  - xvfb

before_script:
  - sleep 3 # give xvfb some time to start
  - npm install
